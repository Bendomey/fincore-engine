type: object
properties:
  errors:
    type: object
    properties:
      name:
        type: string
        example: Failed validation rule 'minLength'
      type:
        type: string
        example: Failed validation rule 'enum'