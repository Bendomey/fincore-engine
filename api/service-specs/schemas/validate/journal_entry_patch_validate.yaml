type: object
properties:
  errors:
    type: object
    properties:
      transaction_date:
        type: string
        example: Failed validation rule 'date'
      metadata:
        type: string
        example: Failed validation rule 'json'