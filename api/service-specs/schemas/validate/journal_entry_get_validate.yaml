type: object
properties:
  errors:
    type: object
    properties:
      client_id:
        type: string
        example: Failed validation rule 'uuid'
      populate:
        type: string
        example: Failed validation rule 'oneof'